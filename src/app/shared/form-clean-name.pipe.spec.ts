import { FormCleanNamePipe } from './form-clean-name.pipe';

describe('FormCleanNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormCleanNamePipe();
    expect(pipe).toBeTruthy();
  });
});
